Options -MultiViews
RewriteEngine On
RewriteBase /

# Let real files and directories load normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route SPA requests through the React entry point.
RewriteRule ^ index.html [L]
